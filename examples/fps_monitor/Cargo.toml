[package]
name = "fps_monitor"
description = "A performance HUD overlaid on an adjustable animation load, using gpui-fps."
version = "0.6.5"
publish = false
edition.workspace = true

[dependencies]
gpui-kit = { path = "../../crates/kit", default-features = false }
gpui-fps.workspace = true

[lints]
workspace = true
