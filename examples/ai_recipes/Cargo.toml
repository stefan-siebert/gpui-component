[package]
name = "gpui-kit-recipes"
description = "Tested consumer recipes published verbatim in the documentation."
version = "0.6.5"
publish = false
edition.workspace = true

[dependencies]
gpui-kit.workspace = true

[dev-dependencies]
gpui-kit = { workspace = true, features = ["test-support"] }

[lints]
workspace = true
