{
  "name": "gpui-component-story-web",
  "version": "0.5.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --bun vite",
    "build": "bun --bun vite build",
    "preview": "bun --bun vite preview",
    "fonts": "python3 ../scripts/subset-fonts.py",
    "wasm": "../scripts/build-wasm.sh --release"
  },
  "devDependencies": {
    "vite": "^8",
    "vite-plugin-static-copy": "^3.2.0",
    "vite-plugin-wasm": "^3.3.0"
  }
}
